{
  "name": "contracts",
  "version": "1.0.0",
  "description": "Not wired up yet. `api/app/services/blockchain.py` is the seam: it returns realistic fake data today so the API and UI can be built and demoed, and it's the only file that needs to change once this phase starts.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^18.0.2",
    "hardhat": "^2.29.1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.6.1"
  }
}
